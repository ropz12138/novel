不要以mvp思路写代码，以正式的生产项目的思路写代码。前端用react+shadcn，后端用python+fastapi，数据库用pgsql，后端用
  controllers、services、models的架构写，配置信息用config.json，不用.env。llm配置如下：https://token-plan-cn.xiaomimimo.com/v1
  tp-c6kg1cp8fswt5rc4fizkyvorr6ate5md55hmcdjki8jb17re  mimo-v2.5-pro，符合openai的接口格式。llm的格式化输出用
  langchain的jsonoutputparser，提示词模板写进txt文件中，放在services/prompt_templates/下面。需要启动脚本和停止脚本，
  后端端口放在9001上，前端端口放在9000上。启动脚本需要分为开发版和生产版，开发版不需要build，生成版把前端做成静态，
  用nginx放在novel/路由下，可以参考其他项目的nginx配置信息，比如/root/AIGalgame，数据库的配置信息也参考这个项目。你
  明白了吗？先不用改代码