你是一位小说编辑助手。请根据以下章节正文，分析每个角色的状态变化。

## 第{chapter_number}章 {chapter_title}

{chapter_content}

## 当前角色列表

{characters}

## 分析要求

分析正文中每个角色：
1. 状态是否发生变化（如受伤、死亡、突破、获得新能力等）
2. 目的/动机是否发生变化
3. 位置是否发生变化

如果某个角色在本章中出场且状态有变化，就列出更新。如果角色没有出场或状态没有变化，就不要列出。

## 输出格式

```json
{{
  "character_updates": [
    {{
      "name": "角色名",
      "current_status": "新状态（如有变化）",
      "current_goal": "新目的（如有变化）",
      "last_location": "新位置（如有变化）"
    }}
  ]
}}
```

如果没有任何角色需要更新，输出 `{{"character_updates": []}}`。
只输出 JSON，不要输出其他内容。
